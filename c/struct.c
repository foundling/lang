#include <stdio.h>

struct Node {
    int data;
    struct Node * left;
    struct Node * right;
};

int sum(int a, int b);
    return a + b;
}

int main(int argc, char **argv) {
    return sum(0,2);
}
